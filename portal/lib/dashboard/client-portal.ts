/**
 * Single client dashboard (`/[slug]`) — same layout for every brand.
 * Coordinator and client logins both use this route.
 */
export const CLIENT_PORTAL_CONTENT_CLASS =
  "mx-auto w-full max-w-5xl px-4 py-10";
