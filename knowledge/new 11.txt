<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Everest Wealth - Vertical Ad</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;700;900&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', -apple-system, sans-serif;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #050505;
        }

        /* 9:16 Fintech Container */
        .ad-container {
            position: relative;
            width: 360px;
            height: 640px;
            background-color: #000000;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 40px 80px rgba(0,0,0,0.9), inset 0 0 0 1px rgba(255,215,0,0.15);
        }

        /* Hero Image Layer */
        .hero-image {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            /* Replace with your abstract dark/gold tech image */
            background-image: url('https://images.unsplash.com/photo-1614028674026-a65e31bfd27c?q=80&w=1000&auto=format&fit=crop'); 
            background-size: cover;
            background-position: center;
            z-index: 1;
            opacity: 0.6; /* Darkened to let the gold UI pop */
        }

        /* Ambient Gold OLED Glow */
        .gold-aura {
            position: absolute;
            bottom: -50px;
            left: 50%;
            transform: translateX(-50%);
            width: 300px;
            height: 300px;
            background: #D4AF37; /* Metallic Gold */
            filter: blur(100px);
            opacity: 0.25;
            z-index: 2;
            border-radius: 50%;
        }

        /* Top Trust Badge (FSP License) */
        .trust-badge {
            position: absolute;
            top: 24px;
            left: 50%;
            transform: translateX(-50%);
            background: rgba(10, 10, 10, 0.6);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid rgba(212, 175, 55, 0.3);
            padding: 6px 16px;
            border-radius: 30px;
            z-index: 10;
            color: #D4AF37;
            font-size: 9px;
            font-weight: 800;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        /* Content Area */
        .content {
            position: absolute;
            bottom: 24px;
            left: 20px;
            right: 20px;
            z-index: 10;
            display: flex;
            flex-direction: column;
        }

        .brand-name {
            font-size: 11px;
            color: rgba(255,255,255,0.7);
            font-weight: 600;
            letter-spacing: 3px;
            text-transform: uppercase;
            margin-bottom: 4px;
        }

        /* Huge Apple-style typography with Samsung Gold gradient */
        .product-title {
            font-size: 46px;
            font-weight: 900;
            letter-spacing: -2px;
            line-height: 1;
            margin-bottom: 8px;
            background: linear-gradient(135deg, #F3E5AB 0%, #D4AF37 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .tagline {
            font-size: 13px;
            color: #ffffff;
            font-weight: 300;
            line-height: 1.4;
            margin-bottom: 20px;
        }

        /* Hardware-style Data Widgets */
        .data-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px;
            margin-bottom: 20px;
        }

        .data-widget {
            background: rgba(15, 15, 15, 0.65);
            border: 1px solid rgba(212, 175, 55, 0.2);
            border-top: 1px solid rgba(212, 175, 55, 0.4); /* Catches fake light */
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            padding: 14px;
            border-radius: 16px;
            display: flex;
            flex-direction: column;
        }

        .widget-label {
            font-size: 9px;
            color: rgba(255,255,255,0.5);
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 4px;
        }

        .widget-value {
            font-size: 14px;
            color: #ffffff;
            font-weight: 700;
        }

        .widget-highlight {
            color: #D4AF37;
        }

        /* Action Row */
        .action-row {
            width: 100%;
        }

        .cta-btn {
            display: block;
            width: 100%;
            text-align: center;
            background: linear-gradient(135deg, #F3E5AB 0%, #D4AF37 100%);
            color: #000000;
            text-decoration: none;
            padding: 16px;
            border-radius: 30px;
            font-size: 13px;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            box-shadow: 0 10px 30px rgba(212, 175, 55, 0.2);
            transition: transform 0.2s;
        }

        .cta-btn:hover {
            transform: translateY(-2px);
        }
    </style>
</head>
<body>

    <div class="ad-container">
        <div class="hero-image"></div>
        <div class="gold-aura"></div>

        <div class="trust-badge">FSP 795 & 17273</div>

        <div class="content">
            <div class="brand-name">Everest Wealth</div>
            <h1 class="product-title">Onyx 14.2%</h1>
            <p class="tagline">Private equity yields. Monthly liquidity. Shielded from public market volatility.</p>

            <div class="data-grid">
                <div class="data-widget">
                    <span class="widget-label">Targeted Yield</span>
                    <span class="widget-value widget-highlight">14.2% P.A.</span>
                </div>
                <div class="data-widget">
                    <span class="widget-label">Distribution</span>
                    <span class="widget-value">Monthly</span>
                </div>
                <div class="data-widget">
                    <span class="widget-label">Broker Fees</span>
                    <span class="widget-value">0% Upfront</span>
                </div>
                <div class="data-widget">
                    <span class="widget-label">Tax Structure</span>
                    <span class="widget-value">20% DWT</span>
                </div>
            </div>

            <div class="action-row">
                <a href="#" class="cta-btn">Secure Your Capital</a>
            </div>
        </div>
    </div>

</body>
</html>


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Everest Wealth - Square Ad</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;700;900&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', -apple-system, sans-serif;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #050505;
        }

        /* 1:1 Fintech Container */
        .ad-container {
            position: relative;
            width: 500px;
            height: 500px;
            background-color: #000000;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 40px 80px rgba(0,0,0,0.9), inset 0 0 0 1px rgba(212, 175, 55, 0.15);
            display: flex;
        }

        /* Right Side Abstract Image */
        .hero-image {
            position: absolute;
            top: 0;
            right: 0;
            width: 55%;
            height: 100%;
            background-image: url('https://images.unsplash.com/photo-1614028674026-a65e31bfd27c?q=80&w=1000&auto=format&fit=crop');
            background-size: cover;
            background-position: left center;
            z-index: 1;
            opacity: 0.7;
        }

        /* Seamless fade to absolute black */
        .fade-layer {
            position: absolute;
            top: 0;
            right: 0;
            width: 60%;
            height: 100%;
            background: linear-gradient(to right, #000000 0%, rgba(0,0,0,0.9) 30%, transparent 100%);
            z-index: 2;
        }

        /* Gold OLED Aura */
        .gold-aura {
            position: absolute;
            top: 50%;
            left: -50px;
            transform: translateY(-50%);
            width: 300px;
            height: 300px;
            background: #D4AF37;
            filter: blur(100px);
            opacity: 0.15;
            z-index: 3;
            border-radius: 50%;
        }

        /* Left Side Content */
        .content {
            position: relative;
            z-index: 10;
            width: 60%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 40px;
        }

        .trust-badge {
            display: inline-flex;
            align-self: flex-start;
            background: rgba(10, 10, 10, 0.6);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(212, 175, 55, 0.3);
            padding: 6px 14px;
            border-radius: 30px;
            color: #D4AF37;
            font-size: 9px;
            font-weight: 800;
            letter-spacing: 2px;
            text-transform: uppercase;
            margin-bottom: 24px;
        }

        .brand-name {
            font-size: 11px;
            color: rgba(255,255,255,0.7);
            font-weight: 600;
            letter-spacing: 3px;
            text-transform: uppercase;
            margin-bottom: 4px;
        }

        .product-title {
            font-size: 48px;
            font-weight: 900;
            letter-spacing: -2px;
            line-height: 0.95;
            margin-bottom: 12px;
            background: linear-gradient(135deg, #F3E5AB 0%, #D4AF37 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            white-space: nowrap;
        }

        .tagline {
            font-size: 13px;
            color: #ffffff;
            font-weight: 300;
            line-height: 1.5;
            margin-bottom: 30px;
            max-width: 95%;
        }

        /* Minimalist Spec Lines */
        .spec-list {
            display: flex;
            flex-direction: column;
            gap: 12px;
            margin-bottom: 32px;
            border-left: 2px solid rgba(212, 175, 55, 0.4);
            padding-left: 16px;
        }

        .spec-item {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .spec-label {
            font-size: 10px;
            color: rgba(255,255,255,0.5);
            text-transform: uppercase;
            letter-spacing: 1px;
            width: 80px;
        }

        .spec-value {
            font-size: 12px;
            color: #ffffff;
            font-weight: 700;
        }

        .highlight {
            color: #D4AF37;
        }

        /* CTA */
        .cta-btn {
            display: inline-block;
            background: linear-gradient(135deg, #F3E5AB 0%, #D4AF37 100%);
            color: #000000;
            text-decoration: none;
            padding: 14px 32px;
            border-radius: 40px;
            font-size: 12px;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            box-shadow: 0 10px 20px rgba(212, 175, 55, 0.2);
            transition: transform 0.2s;
            align-self: flex-start;
        }

        .cta-btn:hover {
            transform: translateY(-2px);
        }
    </style>
</head>
<body>

    <div class="ad-container">
        <div class="hero-image"></div>
        <div class="fade-layer"></div>
        <div class="gold-aura"></div>

        <div class="content">
            <div class="trust-badge">FSP 795 & 17273</div>
            
            <div class="brand-name">Everest Wealth</div>
            <h1 class="product-title">Onyx 14.2%</h1>
            <p class="tagline">Escape public market volatility. Access institutional private equity yields with zero upfront broker fees.</p>

            <div class="spec-list">
                <div class="spec-item">
                    <span class="spec-label">Target Yield</span>
                    <span class="spec-value highlight">14.2% P.A.</span>
                </div>
                <div class="spec-item">
                    <span class="spec-label">Distribution</span>
                    <span class="spec-value">Monthly Income</span>
                </div>
                <div class="spec-item">
                    <span class="spec-label">Tax Benefit</span>
                    <span class="spec-value">20% DWT Structure</span>
                </div>
            </div>

            <a href="#" class="cta-btn">View Portfolio</a>
        </div>
    </div>

</body>
</html>



<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Swiss Vault - Vertical</title>
    <style>
        /* Importing a Serif for the numbers (Playfair Display) and Sans for text (Inter) */
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&family=Playfair+Display:ital,wght@0,600;0,800;1,600&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #050505;
            font-family: 'Inter', -apple-system, sans-serif;
        }

        /* 9:16 Container */
        .ad-container {
            position: relative;
            width: 360px;
            height: 640px;
            /* Deep Institutional Emerald Green */
            background-color: #011C12;
            background-image: radial-gradient(circle at 50% 0%, #033A26 0%, #011C12 80%);
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 40px 80px rgba(0,0,0,0.9), inset 0 0 0 1px rgba(255,255,255,0.1);
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 30px 20px;
        }

        /* Abstract texture overlay */
        .texture-layer {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('https://images.unsplash.com/photo-1550684848-fac1c5b4e853?q=80&w=1000&auto=format&fit=crop'); /* Placeholder for metal/velvet texture */
            background-size: cover;
            background-position: center;
            opacity: 0.15;
            mix-blend-mode: overlay;
            z-index: 1;
        }

        /* Top Branding */
        .header {
            position: relative;
            z-index: 10;
            width: 100%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 40px;
        }

        .brand-name {
            color: #E5E4E2; /* Platinum */
            font-size: 11px;
            font-weight: 700;
            letter-spacing: 3px;
            text-transform: uppercase;
        }

        .fsp-badge {
            color: rgba(229, 228, 226, 0.5);
            font-size: 8px;
            font-weight: 600;
            letter-spacing: 1px;
            text-transform: uppercase;
            border: 1px solid rgba(229, 228, 226, 0.2);
            padding: 4px 8px;
            border-radius: 4px;
        }

        /* The Vault Card (Floating Plaque) */
        .vault-card {
            position: relative;
            z-index: 10;
            width: 100%;
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.15);
            border-top: 1px solid rgba(255, 255, 255, 0.3); /* Catching light */
            border-radius: 16px;
            padding: 32px 24px;
            box-shadow: 0 30px 60px rgba(0,0,0,0.5);
            text-align: center;
        }

        .card-subtitle {
            font-size: 10px;
            color: rgba(229, 228, 226, 0.7);
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 8px;
        }

        /* The Hero Number using Serif Font for Institutional Authority */
        .yield-number {
            font-family: 'Playfair Display', serif;
            font-size: 64px;
            font-weight: 800;
            color: #FFFFFF;
            line-height: 1;
            margin-bottom: 4px;
            text-shadow: 0 10px 20px rgba(0,0,0,0.5);
        }

        .yield-label {
            font-size: 12px;
            color: #E5E4E2;
            font-weight: 400;
            letter-spacing: 1px;
            margin-bottom: 24px;
        }

        /* Ledger-style Divider */
        .divider {
            width: 100%;
            height: 1px;
            background: linear-gradient(90deg, transparent 0%, rgba(255,255,255,0.2) 50%, transparent 100%);
            margin-bottom: 24px;
        }

        /* Strict, clean specs */
        .ledger-specs {
            display: flex;
            flex-direction: column;
            gap: 12px;
            text-align: left;
        }

        .spec-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .spec-title {
            font-size: 11px;
            color: rgba(229, 228, 226, 0.6);
            font-weight: 400;
        }

        .spec-value {
            font-size: 11px;
            color: #FFFFFF;
            font-weight: 600;
            letter-spacing: 0.5px;
        }

        /* Bottom Action Area */
        .footer {
            position: relative;
            z-index: 10;
            width: 100%;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            justify-content: flex-end;
        }

        .tagline {
            font-size: 12px;
            color: rgba(229, 228, 226, 0.8);
            text-align: center;
            line-height: 1.5;
            margin-bottom: 20px;
        }

        .cta-btn {
            display: block;
            width: 100%;
            background: #E5E4E2; /* Solid Platinum */
            color: #011C12; /* Dark Emerald text */
            text-align: center;
            padding: 16px;
            border-radius: 8px; /* Sharper corners for serious tone */
            font-size: 12px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 2px;
            text-decoration: none;
            transition: all 0.2s;
        }

        .cta-btn:hover {
            background: #FFFFFF;
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.3);
        }
    </style>
</head>
<body>

    <div class="ad-container">
        <div class="texture-layer"></div>

        <div class="header">
            <div class="brand-name">Everest Wealth</div>
            <div class="fsp-badge">FSP 795</div>
        </div>

        <div class="vault-card">
            <div class="card-subtitle">Onyx Income+ Portfolio</div>
            <div class="yield-number">14.2<span style="font-size: 40px;">%</span></div>
            <div class="yield-label">Targeted Annual Yield</div>
            
            <div class="divider"></div>

            <div class="ledger-specs">
                <div class="spec-row">
                    <span class="spec-title">Income Distribution</span>
                    <span class="spec-value">Monthly</span>
                </div>
                <div class="spec-row">
                    <span class="spec-title">Upfront Broker Fees</span>
                    <span class="spec-value">0.00%</span>
                </div>
                <div class="spec-row">
                    <span class="spec-title">Tax Efficiency</span>
                    <span class="spec-value">20% DWT</span>
                </div>
            </div>
        </div>

        <div class="footer">
            <p class="tagline">Bypass public market volatility. Access strictly managed private equity.</p>
            <a href="#" class="cta-btn">Initiate Transfer</a>
        </div>
    </div>

</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Swiss Vault - Square</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&family=Playfair+Display:ital,wght@0,600;0,800;1,600&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #050505;
            font-family: 'Inter', -apple-system, sans-serif;
        }

        /* 1:1 Container */
        .ad-container {
            position: relative;
            width: 500px;
            height: 500px;
            background-color: #011C12; /* Deep Emerald */
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 40px 80px rgba(0,0,0,0.9), inset 0 0 0 1px rgba(255,255,255,0.1);
            display: flex;
        }

        /* Background Texture */
        .texture-layer {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('https://images.unsplash.com/photo-1550684848-fac1c5b4e853?q=80&w=1000&auto=format&fit=crop');
            background-size: cover;
            background-position: center;
            opacity: 0.15;
            mix-blend-mode: overlay;
            z-index: 1;
        }

        /* Left Panel - The Art (Massive Typography) */
        .left-panel {
            position: relative;
            z-index: 10;
            width: 55%;
            height: 100%;
            padding: 40px 0 40px 40px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            border-right: 1px solid rgba(255, 255, 255, 0.1);
            background: linear-gradient(90deg, rgba(3,58,38,0.8) 0%, rgba(1,28,18,0.8) 100%);
        }

        .brand-name {
            color: #E5E4E2;
            font-size: 11px;
            font-weight: 700;
            letter-spacing: 3px;
            text-transform: uppercase;
        }

        .yield-container {
            margin-top: -20px;
        }

        .portfolio-name {
            font-size: 12px;
            color: rgba(229, 228, 226, 0.6);
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 8px;
        }

        /* Massive Serif Number */
        .yield-number {
            font-family: 'Playfair Display', serif;
            font-size: 88px;
            font-weight: 800;
            color: #FFFFFF;
            line-height: 0.85;
            margin-bottom: 8px;
            letter-spacing: -2px;
        }

        .yield-percent {
            font-size: 50px;
            color: rgba(255,255,255,0.8);
        }

        .yield-label {
            font-size: 13px;
            color: #E5E4E2;
            font-weight: 300;
            letter-spacing: 0.5px;
            max-width: 80%;
            line-height: 1.4;
        }

        /* Right Panel - The Ledger (Data & Specs) */
        .right-panel {
            position: relative;
            z-index: 10;
            width: 45%;
            height: 100%;
            padding: 40px 30px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            background: rgba(0, 0, 0, 0.2);
            backdrop-filter: blur(10px);
        }

        .ledger-table {
            display: flex;
            flex-direction: column;
        }

        .ledger-row {
            padding: 16px 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .ledger-row:first-child {
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .ledger-title {
            display: block;
            font-size: 9px;
            color: rgba(229, 228, 226, 0.5);
            text-transform: uppercase;
            letter-spacing: 1.5px;
            margin-bottom: 4px;
        }

        .ledger-value {
            display: block;
            font-size: 14px;
            color: #FFFFFF;
            font-weight: 600;
            letter-spacing: 0.5px;
        }

        .cta-btn {
            display: block;
            width: 100%;
            background: #E5E4E2;
            color: #011C12;
            text-align: center;
            padding: 16px;
            border-radius: 4px; /* Brutalist/Structural */
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 2px;
            text-decoration: none;
            transition: all 0.2s;
        }

        .cta-btn:hover {
            background: #FFFFFF;
            box-shadow: 0 10px 20px rgba(0,0,0,0.3);
        }
    </style>
</head>
<body>

    <div class="ad-container">
        <div class="texture-layer"></div>

        <div class="left-panel">
            <div class="brand-name">Everest Wealth</div>
            
            <div class="yield-container">
                <div class="portfolio-name">Onyx Income+</div>
                <div class="yield-number">14.2<span class="yield-percent">%</span></div>
                <div class="yield-label">Targeted Annual Yield.<br>Shielded from public volatility.</div>
            </div>
            
            <div style="font-size: 9px; color: rgba(255,255,255,0.3); text-transform: uppercase;">FSP 795 & 17273</div>
        </div>

        <div class="right-panel">
            <div class="ledger-table">
                <div class="ledger-row">
                    <span class="ledger-title">Income Distribution</span>
                    <span class="ledger-value">Monthly</span>
                </div>
                <div class="ledger-row">
                    <span class="ledger-title">Upfront Broker Fees</span>
                    <span class="ledger-value">0.00%</span>
                </div>
                <div class="ledger-row" style="border-bottom: none;">
                    <span class="ledger-title">Tax Structure</span>
                    <span class="ledger-value">20% DWT</span>
                </div>
            </div>

            <a href="#" class="cta-btn">Review Portfolio</a>
        </div>
    </div>

</body>
</html>



<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cobalt Monolith - Vertical</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800;900&family=JetBrains+Mono:wght@400;700&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', -apple-system, sans-serif;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #050505;
        }

        /* 9:16 Container */
        .ad-container {
            position: relative;
            width: 360px;
            height: 640px;
            background-color: #000000;
            border-radius: 12px; /* Sharper corners for a tech feel */
            overflow: hidden;
            box-shadow: 0 40px 80px rgba(0,0,0,0.9);
            /* Electric Blue subtle inner border */
            border: 1px solid rgba(0, 102, 255, 0.3);
        }

        /* Hero Image Layer */
        .hero-image {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?q=80&w=1000&auto=format&fit=crop'); 
            background-size: cover;
            background-position: center;
            z-index: 1;
            opacity: 0.5; /* Darken heavily for text contrast */
        }

        /* Bottom fade to pure black */
        .gradient-floor {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 60%;
            background: linear-gradient(to top, #000000 0%, #000000 40%, transparent 100%);
            z-index: 2;
        }

        /* Top HUD Elements */
        .top-hud {
            position: absolute;
            top: 24px;
            left: 24px;
            right: 24px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 10;
        }

        .brand-name {
            color: #C0C0C0; /* Silver */
            font-size: 11px;
            font-weight: 800;
            letter-spacing: 4px;
            text-transform: uppercase;
        }

        .license-badge {
            font-family: 'JetBrains Mono', monospace;
            color: #0066FF; /* Cobalt Blue */
            font-size: 9px;
            background: rgba(0, 102, 255, 0.1);
            border: 1px solid rgba(0, 102, 255, 0.4);
            padding: 4px 8px;
            border-radius: 4px;
        }

        /* Core Content Area */
        .content {
            position: absolute;
            bottom: 30px;
            left: 24px;
            right: 24px;
            z-index: 10;
            display: flex;
            flex-direction: column;
        }

        /* Cobalt Glowing Accent Line */
        .accent-line {
            width: 40px;
            height: 4px;
            background: #0066FF;
            box-shadow: 0 0 15px #0066FF;
            margin-bottom: 20px;
            border-radius: 2px;
        }

        .product-class {
            font-family: 'JetBrains Mono', monospace;
            font-size: 10px;
            color: #888888;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 8px;
        }

        /* Silver/Titanium Gradient Title */
        .product-title {
            font-size: 42px;
            font-weight: 900;
            letter-spacing: -1.5px;
            line-height: 1;
            margin-bottom: 12px;
            background: linear-gradient(135deg, #FFFFFF 0%, #888888 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .tagline {
            font-size: 13px;
            color: #C0C0C0;
            font-weight: 300;
            line-height: 1.4;
            margin-bottom: 24px;
        }

        /* Tech-style Specs */
        .spec-container {
            display: flex;
            gap: 16px;
            margin-bottom: 24px;
            border-left: 1px solid rgba(255,255,255,0.1);
            padding-left: 16px;
        }

        .spec-item {
            display: flex;
            flex-direction: column;
        }

        .spec-label {
            font-size: 9px;
            color: #666666;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 4px;
        }

        .spec-value {
            font-size: 13px;
            color: #FFFFFF;
            font-weight: 700;
        }

        .highlight {
            color: #0066FF;
        }

        /* Razor sharp CTA */
        .cta-btn {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
            background: #0066FF; /* Pure Cobalt */
            color: #FFFFFF;
            text-decoration: none;
            padding: 14px;
            border-radius: 6px; /* Squarish for tech look */
            font-size: 12px;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 2px;
            box-shadow: 0 10px 20px rgba(0, 102, 255, 0.25);
            transition: all 0.2s;
        }

        .cta-btn:hover {
            background: #0052CC;
            box-shadow: 0 10px 30px rgba(0, 102, 255, 0.5);
            transform: translateY(-2px);
        }
    </style>
</head>
<body>

    <div class="ad-container">
        <div class="hero-image"></div>
        <div class="gradient-floor"></div>

        <div class="top-hud">
            <div class="brand-name">Everest Wealth</div>
            <div class="license-badge">FSP_795</div>
        </div>

        <div class="content">
            <div class="accent-line"></div>
            
            <div class="product-class">Class B Share</div>
            <h1 class="product-title">Strategic Growth 14.5%</h1>
            <p class="tagline">Pure capital compounding. Absolute liquidity lock for aggressive multi-year expansion.</p>

            <div class="spec-container">
                <div class="spec-item">
                    <span class="spec-label">Target Return</span>
                    <span class="spec-value highlight">14.50% P.A.</span>
                </div>
                <div class="spec-item">
                    <span class="spec-label">Distribution</span>
                    <span class="spec-value">Maturity (Year 5)</span>
                </div>
            </div>

            <a href="#" class="cta-btn">Initialize Investment</a>
        </div>
    </div>

</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Terminal Interface - Square</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800;900&family=JetBrains+Mono:wght@400;700&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', -apple-system, sans-serif;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #050505;
        }

        /* 1:1 Terminal Container */
        .ad-container {
            position: relative;
            width: 500px;
            height: 500px;
            background-color: #000000;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 40px 80px rgba(0,0,0,0.9);
            border: 1px solid rgba(0, 102, 255, 0.2);
            display: flex;
        }

        /* Right Side Image */
        .hero-image {
            position: absolute;
            top: 0;
            right: 0;
            width: 60%;
            height: 100%;
            background-image: url('https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?q=80&w=1000&auto=format&fit=crop');
            background-size: cover;
            background-position: center;
            z-index: 1;
            opacity: 0.6;
        }

        /* Hard Terminal Fade */
        .fade-layer {
            position: absolute;
            top: 0;
            right: 0;
            width: 60%;
            height: 100%;
            background: linear-gradient(to right, #000000 0%, #000000 20%, transparent 100%);
            z-index: 2;
        }

        /* Cobalt Glow Aura */
        .cobalt-aura {
            position: absolute;
            top: 50%;
            left: -50px;
            transform: translateY(-50%);
            width: 300px;
            height: 300px;
            background: #0066FF;
            filter: blur(120px);
            opacity: 0.25;
            z-index: 3;
            border-radius: 50%;
        }

        /* Left Side Terminal Content */
        .content {
            position: relative;
            z-index: 10;
            width: 65%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 40px;
        }

        /* System Header */
        .system-header {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 24px;
        }

        .brand-name {
            color: #C0C0C0;
            font-size: 10px;
            font-weight: 800;
            letter-spacing: 3px;
            text-transform: uppercase;
        }

        .license-badge {
            font-family: 'JetBrains Mono', monospace;
            color: #0066FF;
            font-size: 8px;
            background: rgba(0, 102, 255, 0.1);
            border: 1px solid rgba(0, 102, 255, 0.4);
            padding: 3px 6px;
            border-radius: 3px;
        }

        /* Typography */
        .product-class {
            font-family: 'JetBrains Mono', monospace;
            font-size: 11px;
            color: #0066FF;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 6px;
        }

        .product-title {
            font-size: 46px;
            font-weight: 900;
            letter-spacing: -1.5px;
            line-height: 1;
            margin-bottom: 16px;
            background: linear-gradient(135deg, #FFFFFF 0%, #888888 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .tagline {
            font-size: 12px;
            color: #A0A0A0;
            font-weight: 400;
            line-height: 1.5;
            margin-bottom: 30px;
            max-width: 90%;
        }

        /* Terminal Data Grid */
        .data-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 12px;
            margin-bottom: 30px;
        }

        .data-row {
            display: flex;
            align-items: center;
            border-bottom: 1px dotted rgba(255,255,255,0.15);
            padding-bottom: 8px;
        }

        .data-label {
            font-family: 'JetBrains Mono', monospace;
            font-size: 9px;
            color: #666666;
            text-transform: uppercase;
            width: 120px;
        }

        .data-value {
            font-size: 13px;
            color: #FFFFFF;
            font-weight: 700;
        }

        .highlight {
            color: #0066FF;
        }

        /* CTA */
        .cta-btn {
            display: inline-block;
            background: rgba(0, 102, 255, 0.1);
            color: #0066FF;
            text-decoration: none;
            padding: 12px 28px;
            border: 1px solid #0066FF;
            border-radius: 4px;
            font-size: 11px;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 2px;
            transition: all 0.2s;
            align-self: flex-start;
        }

        .cta-btn:hover {
            background: #0066FF;
            color: #FFFFFF;
            box-shadow: 0 0 20px rgba(0, 102, 255, 0.4);
        }
    </style>
</head>
<body>

    <div class="ad-container">
        <div class="hero-image"></div>
        <div class="fade-layer"></div>
        <div class="cobalt-aura"></div>

        <div class="content">
            <div class="system-header">
                <div class="brand-name">Everest Wealth</div>
                <div class="license-badge">FSP_795</div>
            </div>

            <div class="product-class">Class B Structure</div>
            <h1 class="product-title">Strategic<br>Growth.</h1>
            <p class="tagline">Zero monthly withdrawals permitted. Built exclusively for aggressive 5-year capital compounding.</p>

            <div class="data-grid">
                <div class="data-row">
                    <span class="data-label">Target_Yield</span>
                    <span class="data-value highlight">14.50% P.A.</span>
                </div>
                <div class="data-row">
                    <span class="data-label">Distribution</span>
                    <span class="data-value">At Maturity (Yr 5)</span>
                </div>
                <div class="data-row">
                    <span class="data-label">Upfront_Fees</span>
                    <span class="data-value">0.00%</span>
                </div>
            </div>

            <a href="#" class="cta-btn">Initialize</a>
        </div>
    </div>

</body>
</html>



<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Horology Blueprint - Vertical</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@200;300;400;600&family=Playfair+Display:ital,wght@0,400;0,600;0,800;1,400&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #02040A;
        }

        /* 9:16 Luxury Container */
        .ad-container {
            position: relative;
            width: 360px;
            height: 640px;
            background-color: #020611; /* Deep Midnight Navy */
            border-radius: 0px; /* Sharp, tailored edges. No playful curves. */
            overflow: hidden;
            box-shadow: 0 40px 80px rgba(0,0,0,0.9);
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        /* The Inner Platinum Bezel */
        .inner-bezel {
            position: absolute;
            top: 12px;
            left: 12px;
            right: 12px;
            bottom: 12px;
            border: 1px solid rgba(224, 229, 236, 0.25); /* Brushed Silver */
            z-index: 10;
            pointer-events: none;
        }

        /* Hero Image Layer */
        .hero-image {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('https://images.unsplash.com/photo-1548036328-c9fa89d128fa?q=80&w=1000&auto=format&fit=crop'); /* Placeholder: Watch mechanism/dark luxury */
            background-size: cover;
            background-position: center;
            z-index: 1;
            opacity: 0.4; /* Highly darkened */
            mix-blend-mode: luminosity; /* Strips cheap colors, keeps it metallic */
        }

        /* Vignette to protect text */
        .vignette {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at center, transparent 20%, #020611 100%);
            z-index: 2;
        }

        /* Content Stage */
        .content {
            position: relative;
            z-index: 15;
            width: 100%;
            height: 100%;
            padding: 40px 24px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            align-items: center;
            text-align: center;
        }

        /* Top: Brand */
        .brand-block {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 8px;
        }

        .brand-name {
            font-family: 'Montserrat', sans-serif;
            font-size: 10px;
            color: #E0E5EC; /* Platinum */
            font-weight: 300;
            letter-spacing: 6px;
            text-transform: uppercase;
        }

        .brand-line {
            width: 20px;
            height: 1px;
            background-color: #4A90E2; /* Ice Blue Hint */
        }

        /* Middle: The Core Offer */
        .offer-block {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .product-name {
            font-family: 'Playfair Display', serif;
            font-style: italic;
            font-size: 16px;
            color: rgba(224, 229, 236, 0.7);
            margin-bottom: 12px;
        }

        .yield-number {
            font-family: 'Playfair Display', serif;
            font-size: 64px;
            font-weight: 400;
            color: #FFFFFF;
            line-height: 1;
            letter-spacing: -2px;
            margin-bottom: 8px;
        }

        .yield-label {
            font-family: 'Montserrat', sans-serif;
            font-size: 9px;
            color: #E0E5EC;
            font-weight: 200;
            letter-spacing: 3px;
            text-transform: uppercase;
        }

        /* Bottom: Specs & CTA */
        .details-block {
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .spec-list {
            display: flex;
            width: 80%;
            justify-content: space-between;
            border-top: 1px solid rgba(224, 229, 236, 0.15);
            border-bottom: 1px solid rgba(224, 229, 236, 0.15);
            padding: 16px 0;
            margin-bottom: 32px;
        }

        .spec-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 4px;
        }

        .spec-title {
            font-family: 'Montserrat', sans-serif;
            font-size: 7px;
            color: rgba(224, 229, 236, 0.5);
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .spec-value {
            font-family: 'Montserrat', sans-serif;
            font-size: 10px;
            color: #E0E5EC;
            font-weight: 400;
            letter-spacing: 1px;
        }

        /* Sleek, understated CTA */
        .cta-btn {
            font-family: 'Montserrat', sans-serif;
            display: inline-block;
            background: transparent;
            color: #E0E5EC;
            text-decoration: none;
            padding: 12px 32px;
            border: 1px solid #E0E5EC;
            font-size: 9px;
            font-weight: 400;
            text-transform: uppercase;
            letter-spacing: 3px;
            transition: all 0.3s ease;
        }

        .cta-btn:hover {
            background: #E0E5EC;
            color: #020611;
        }
    </style>
</head>
<body>

    <div class="ad-container">
        <div class="inner-bezel"></div>
        <div class="hero-image"></div>
        <div class="vignette"></div>

        <div class="content">
            
            <div class="brand-block">
                <span class="brand-name">Everest Private</span>
                <div class="brand-line"></div>
            </div>

            <div class="offer-block">
                <div class="product-name">The Onyx Portfolio</div>
                <div class="yield-number">14.2%</div>
                <div class="yield-label">Targeted Annual Yield</div>
            </div>

            <div class="details-block">
                <div class="spec-list">
                    <div class="spec-item">
                        <span class="spec-title">Liquidity</span>
                        <span class="spec-value">Monthly</span>
                    </div>
                    <div class="spec-item">
                        <span class="spec-title">Volatility</span>
                        <span class="spec-value">Uncorrelated</span>
                    </div>
                    <div class="spec-item">
                        <span class="spec-title">Broker Fees</span>
                        <span class="spec-value">0.00%</span>
                    </div>
                </div>

                <a href="#" class="cta-btn">Request Prospectus</a>
            </div>

        </div>
    </div>

</body>
</html>


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crystal Plaque - Square</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@200;300;400;600&family=Playfair+Display:ital,wght@0,400;0,600;0,800;1,400&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #02040A;
        }

        /* 1:1 Luxury Container */
        .ad-container {
            position: relative;
            width: 500px;
            height: 500px;
            background-color: #020611;
            border-radius: 0px; 
            overflow: hidden;
            box-shadow: 0 40px 80px rgba(0,0,0,0.9);
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 30px;
        }

        /* Background Image */
        .hero-image {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('https://images.unsplash.com/photo-1548036328-c9fa89d128fa?q=80&w=1000&auto=format&fit=crop'); 
            background-size: cover;
            background-position: center;
            z-index: 1;
            opacity: 0.5;
            mix-blend-mode: luminosity; 
        }

        /* Deep Navy Vignette */
        .vignette {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at center, transparent 0%, #020611 100%);
            z-index: 2;
        }

        /* The Centered Crystal Plaque */
        .crystal-card {
            position: relative;
            z-index: 10;
            width: 100%;
            height: 100%;
            background: rgba(2, 6, 17, 0.4); /* Midnight blue glass */
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            border: 1px solid rgba(224, 229, 236, 0.15);
            border-top: 1px solid rgba(224, 229, 236, 0.3); /* Top edge light catch */
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 40px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.5);
        }

        /* Inner Content */
        .brand-name {
            font-family: 'Montserrat', sans-serif;
            font-size: 11px;
            color: #4A90E2; /* Ice Blue */
            font-weight: 400;
            letter-spacing: 5px;
            text-transform: uppercase;
            margin-bottom: 24px;
        }

        .product-name {
            font-family: 'Playfair Display', serif;
            font-size: 24px;
            color: #E0E5EC;
            margin-bottom: 16px;
        }

        .yield-number {
            font-family: 'Playfair Display', serif;
            font-size: 72px;
            font-weight: 400;
            color: #FFFFFF;
            line-height: 0.9;
            margin-bottom: 24px;
        }

        .yield-label {
            font-family: 'Montserrat', sans-serif;
            font-size: 11px;
            color: rgba(224, 229, 236, 0.7);
            font-weight: 300;
            letter-spacing: 2px;
            text-transform: uppercase;
            margin-bottom: 40px;
            max-width: 80%;
            line-height: 1.6;
        }

        /* CTA */
        .cta-btn {
            font-family: 'Montserrat', sans-serif;
            display: inline-block;
            background: #E0E5EC; /* Solid Platinum */
            color: #020611; /* Midnight Text */
            text-decoration: none;
            padding: 14px 40px;
            font-size: 10px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 3px;
            transition: all 0.3s ease;
        }

        .cta-btn:hover {
            background: #FFFFFF;
            box-shadow: 0 0 20px rgba(224, 229, 236, 0.3);
        }

        /* Tiny FSP text anchored to the bottom of the card */
        .fsp-text {
            position: absolute;
            bottom: 20px;
            font-family: 'Montserrat', sans-serif;
            font-size: 7px;
            color: rgba(224, 229, 236, 0.3);
            letter-spacing: 2px;
            text-transform: uppercase;
        }
    </style>
</head>
<body>

    <div class="ad-container">
        <div class="hero-image"></div>
        <div class="vignette"></div>

        <div class="crystal-card">
            <div class="brand-name">Everest Wealth</div>
            
            <div class="product-name">The Onyx Income+ Portfolio</div>
            <div class="yield-number">14.2%</div>
            
            <div class="yield-label">
                Absolute private equity returns.<br>
                Zero public market volatility.
            </div>

            <a href="#" class="cta-btn">Private Access</a>

            <div class="fsp-text">Authorised FSP 795 & 17273</div>
        </div>
    </div>

</body>
</html>


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Titanium & Sapphire - Vertical</title>
    <style>
        /* Pure Apple/Samsung Typography: Inter */
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;700;900&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #000000;
        }

        /* 9:16 Tech Container */
        .ad-container {
            position: relative;
            width: 360px;
            height: 640px;
            background-color: #000000;
            border-radius: 24px; /* Apple device corner radius */
            overflow: hidden;
            box-shadow: 0 40px 80px rgba(0,0,0,0.9), inset 0 0 0 1px rgba(255,255,255,0.1);
        }

        /* Abstract Titanium Hero Image */
        .hero-image {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 65%;
            background-image: url('https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?q=80&w=1000&auto=format&fit=crop'); 
            background-size: cover;
            background-position: center;
            z-index: 1;
            opacity: 0.6;
        }

        /* Pure Black Fade */
        .fade-floor {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 70%;
            background: linear-gradient(to top, #000000 0%, #000000 45%, transparent 100%);
            z-index: 2;
        }

        /* Electric Blue Accent Glow behind the card */
        .tech-glow {
            position: absolute;
            bottom: 120px;
            left: 50%;
            transform: translateX(-50%);
            width: 200px;
            height: 200px;
            background: #0A84FF; /* Apple System Blue */
            filter: blur(80px);
            opacity: 0.3;
            z-index: 3;
            border-radius: 50%;
        }

        /* Top Bar Branding */
        .top-bar {
            position: absolute;
            top: 24px;
            left: 24px;
            right: 24px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 10;
        }

        .brand-name {
            color: #F5F5F7; /* Apple Off-White */
            font-size: 11px;
            font-weight: 700;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        .badge {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: #0A84FF;
            font-size: 9px;
            font-weight: 700;
            padding: 4px 10px;
            border-radius: 20px;
            letter-spacing: 1px;
        }

        /* Glassmorphic Widget Card (Samsung One UI layout + Apple Glass) */
        .widget-card {
            position: absolute;
            bottom: 24px;
            left: 16px;
            right: 16px;
            background: rgba(20, 20, 22, 0.55);
            backdrop-filter: blur(35px);
            -webkit-backdrop-filter: blur(35px);
            border: 1px solid rgba(255, 255, 255, 0.15);
            border-top: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 24px;
            padding: 28px 24px;
            z-index: 10;
            box-shadow: 0 20px 40px rgba(0,0,0,0.6);
        }

        .portfolio-name {
            font-size: 11px;
            color: #0A84FF;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 6px;
        }

        .yield-text {
            font-size: 48px;
            font-weight: 900;
            color: #F5F5F7;
            letter-spacing: -2px;
            line-height: 1;
            margin-bottom: 4px;
        }

        .yield-subtext {
            font-size: 13px;
            color: #86868B; /* Apple Space Grey text */
            font-weight: 400;
            margin-bottom: 24px;
            line-height: 1.4;
        }

        /* Tech Specs Flexbox */
        .specs-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 24px;
            padding-bottom: 20px;
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }

        .spec {
            display: flex;
            flex-direction: column;
            gap: 4px;
        }

        .spec-label {
            font-size: 10px;
            color: #86868B;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-weight: 500;
        }

        .spec-value {
            font-size: 13px;
            color: #F5F5F7;
            font-weight: 700;
        }

        /* Clean, tech button */
        .cta-btn {
            display: block;
            width: 100%;
            background: #F5F5F7;
            color: #000000;
            text-align: center;
            padding: 14px;
            border-radius: 40px;
            font-size: 13px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            text-decoration: none;
            transition: transform 0.2s;
        }

        .cta-btn:hover {
            transform: scale(1.02);
            background: #FFFFFF;
        }
    </style>
</head>
<body>

    <div class="ad-container">
        
        <div class="hero-image"></div>
        <div class="fade-floor"></div>
        <div class="tech-glow"></div>

        <div class="top-bar">
            <span class="brand-name">Everest Wealth</span>
            <span class="badge">FSP 795</span>
        </div>

        <div class="widget-card">
            <div class="portfolio-name">Onyx Income+</div>
            <div class="yield-text">14.2%</div>
            <p class="yield-subtext">Targeted Annual Yield.<br>Shielded from public volatility.</p>

            <div class="specs-row">
                <div class="spec">
                    <span class="spec-label">Liquidity</span>
                    <span class="spec-value">Monthly</span>
                </div>
                <div class="spec">
                    <span class="spec-label">Broker Fees</span>
                    <span class="spec-value">0.00%</span>
                </div>
                <div class="spec">
                    <span class="spec-label">Tax Base</span>
                    <span class="spec-value">20% DWT</span>
                </div>
            </div>

            <a href="#" class="cta-btn">Initialize Transfer</a>
        </div>

    </div>

</body>
</html>


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Titanium & Sapphire - Square</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;700;900&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #000000;
        }

        /* 1:1 Tech Container */
        .ad-container {
            position: relative;
            width: 500px;
            height: 500px;
            background-color: #000000;
            border-radius: 24px; 
            overflow: hidden;
            box-shadow: 0 40px 80px rgba(0,0,0,0.9), inset 0 0 0 1px rgba(255,255,255,0.1);
            display: flex;
            padding: 32px;
        }

        /* The OLED Glow in the background */
        .ambient-glow {
            position: absolute;
            top: 50%;
            left: 20%;
            transform: translateY(-50%);
            width: 300px;
            height: 300px;
            background: #0A84FF; /* Electric Blue */
            filter: blur(120px);
            opacity: 0.2;
            z-index: 1;
        }

        /* Left Side: The Fintech Dashboard */
        .dashboard-ui {
            position: relative;
            z-index: 10;
            width: 55%;
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .header {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .brand-name {
            color: #F5F5F7;
            font-size: 12px;
            font-weight: 700;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        .badge {
            background: rgba(10, 132, 255, 0.15);
            border: 1px solid rgba(10, 132, 255, 0.3);
            color: #0A84FF;
            font-size: 9px;
            font-weight: 700;
            padding: 4px 8px;
            border-radius: 6px;
        }

        /* Main Data Display */
        .data-display {
            display: flex;
            flex-direction: column;
        }

        .portfolio-label {
            font-size: 12px;
            color: #86868B;
            text-transform: uppercase;
            letter-spacing: 2px;
            font-weight: 600;
            margin-bottom: 8px;
        }

        .yield-number {
            font-size: 64px;
            font-weight: 900;
            color: #F5F5F7;
            letter-spacing: -3px;
            line-height: 1;
            margin-bottom: 12px;
        }

        .tagline {
            font-size: 14px;
            color: #A1A1A6;
            font-weight: 400;
            line-height: 1.4;
            max-width: 95%;
        }

        /* CTA Row */
        .action-row {
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .cta-btn {
            background: #F5F5F7;
            color: #000000;
            text-decoration: none;
            padding: 14px 28px;
            border-radius: 40px;
            font-size: 12px;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.2s ease;
        }

        .cta-btn:hover {
            transform: scale(1.03);
            box-shadow: 0 10px 20px rgba(255,255,255,0.2);
        }

        .fee-label {
            font-size: 11px;
            color: #0A84FF;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* Right Side: The Image Widget (Squircle) */
        .image-widget {
            position: absolute;
            top: 32px;
            right: 32px;
            width: 38%;
            height: calc(100% - 64px);
            border-radius: 32px; /* Smooth iOS style corner */
            background-image: url('https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?q=80&w=1000&auto=format&fit=crop');
            background-size: cover;
            background-position: center;
            z-index: 10;
            box-shadow: 0 30px 60px rgba(0,0,0,0.8), inset 0 0 0 1px rgba(255,255,255,0.2);
        }
    </style>
</head>
<body>

    <div class="ad-container">
        
        <div class="ambient-glow"></div>

        <div class="dashboard-ui">
            <div class="header">
                <span class="brand-name">Everest Wealth</span>
                <span class="badge">FSP_795</span>
            </div>

            <div class="data-display">
                <span class="portfolio-label">Onyx Income+</span>
                <h1 class="yield-number">14.2%</h1>
                <p class="tagline">Escape market volatility. Access strictly managed private equity with monthly distributions.</p>
            </div>

            <div class="action-row">
                <a href="#" class="cta-btn">Review Portfolio</a>
                <span class="fee-label">0% Upfront Fees</span>
            </div>
        </div>

        <div class="image-widget"></div>

    </div>

</body>
</html>